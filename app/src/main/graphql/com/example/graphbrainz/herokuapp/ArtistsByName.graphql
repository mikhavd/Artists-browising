query ArtistsByName($artistName: String!, $limit: Int!) {
    search {
        artists(query: $artistName, first: $limit, after: null){
            nodes {
                id
                name
                disambiguation
            }
        }
    }
}